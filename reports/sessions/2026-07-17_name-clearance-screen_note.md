# Session note — 2026-07-17 — Name clearance screen (picked name) result

Phil picked a name from the v2 mascot-name shortlist. Ran an approximate web-based clearance
screen on it. **The name itself stays in the private repo** (publishing an unlocked, unsecured
name invites squatting — and this screen found the `.com` is gone and handles are contested,
so the risk is live). This note carries the read + the required actions, not the name.

## Screen result (web-approximate — NOT authoritative; counsel still required)
- **Overall: PROCEED TO FORMAL CLEARANCE with a caution flag** — no hard blocker found.
- **Steam exact-title: CLEAR** (no game with the name; store slug looks free).
- **Trademark Class 41 (entertainment): GO** (open, pending authoritative search).
- **Trademark Class 9 (game software): CAUTION** — no exact US game-software mark found, but the
  exact word is already used by a live mobile app + a US-exporting hardware brand in Class 9.
  A **paid USPTO/TSDR + EUIPO knockout + trademark-counsel sign-off is mandatory before lock.**
- **Famous-character confusion: OK** (mild footnote only).
- **Domain/handles: partially encumbered** — the `.com` is taken; the `.io` looks open (verify);
  the Steam slug + most social handles look claimable but the exact word is used on a couple of
  platforms → plan to differentiate the handle set.

## Actions before lock (owner)
1. Paid USPTO Class 9 + 41 (+ EUIPO) knockout + counsel sign-off.
2. Secure the `.io` domain and a differentiated handle set immediately.
3. Only then does the name go on any public asset. (D-3R art is name-independent and proceeds now.)
